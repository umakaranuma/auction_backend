django==4.2.7
djangorestframework==3.14.0
# Required by Django/DRF ImageField for multipart image validation (often present locally but missing on Vercel).
Pillow>=10.3.0,<13
django-cors-headers==4.3.1
python-dotenv==1.0.1
# 2.3.x pins httpx<0.26 but pip can pull newer gotrue that passes proxy= to httpx (needs httpx>=0.26).
supabase==2.28.3
# Pure-Python driver; mysqlclient needs libmysql dev headers (not on Vercel).
PyMySQL==1.1.1
gunicorn==21.2.0
